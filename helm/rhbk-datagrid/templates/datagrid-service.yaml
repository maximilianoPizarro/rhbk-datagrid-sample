{{- if .Values.datagrid.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "rhbk-datagrid.datagrid" . }}
  labels:
    {{- include "rhbk-datagrid.datagridLabels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: 11222
      targetPort: hotrod
      protocol: TCP
      name: hotrod
  selector:
    {{- include "rhbk-datagrid.datagridSelectorLabels" . | nindent 4 }}
{{- end }}
